meta {
  name: Criar Conta
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/contas
  body: json
  auth: none
}

body:json {
  {
    "nome": "Conta Corrente"
  }
}

script:post-response {
  bru.setVar("contaId", res.body.id);
}
